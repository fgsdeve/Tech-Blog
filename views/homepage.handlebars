{{> header}}
<div class="container">
  {{#each posts}}
    <div class="post">
      <h2>{{this.title}}</h2>
      <p>{{this.content}}</p>
      <p>Posted by {{this.username}} on {{this.createdAt}}</p>
    </div>
  {{/each}}
</div>
{{> footer}}
